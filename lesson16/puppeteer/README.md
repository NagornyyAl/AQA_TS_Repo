# Lesson 16 Puppeteer

Puppeteer + Vitest tests based on homework 15 selectors and actions.

## Commands

```bash
npm install
npm test
```

Use PowerShell command `$env:HEADLESS='false'; npm test` when you need to see the browser.
